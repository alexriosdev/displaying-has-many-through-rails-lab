<h1>Patient Index</h1>
<ul>
  <% @patients.each do |patient| %>
    <li>
      <a href="<%=patient_path(patient)%>">
        Name: <%= patient.name %>
        Number of Appointments: <%=patient.appointments.size %> 
      </a>     
    </li>     
  <% end %>
</ul>